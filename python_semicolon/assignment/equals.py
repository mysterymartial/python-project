def equal_string(word, second_word):
	reverse = ''
	if (len(word)) == (len(second_word)):
	          spilt_word == [word]
		  for character in split_word:
				if character in secon_word:
					reverse = reverse + character
			if word == split_word:
			return True


			else:

				return False




word = input("Enter a word\n")
reverse = input("Enter another word \n")
response = equal_string(word, reverse)
print(response)
				